# Dependency versions verified conflict-free on 2026-09-17.
# bedrock-agentcore>=1.x requires starlette>=0.46.2, which is satisfied by
# fastapi>=0.116.0 — so both are unpinned to let pip pick the latest compatible set.

fastapi
uvicorn[standard]
python-multipart
strands-agents
bedrock-agentcore
pymupdf
ffmpeg-python
ollama
