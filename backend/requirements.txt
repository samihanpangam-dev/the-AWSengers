# Dependency versions verified conflict-free.
# bedrock-agentcore >=1.x requires starlette>=0.46.2, satisfied by fastapi>=0.116.0.
# Pillow and pydub added for dynamic synthesis scripts (referenced in system prompt).

fastapi
uvicorn[standard]
python-multipart
strands-agents

bedrock-agentcore
pymupdf
ffmpeg-python
ollama
Pillow
pydub
