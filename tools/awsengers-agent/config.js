/* Optional static deployment configuration. Do not commit real secrets here. */
window.AWSENGERS_AGENT_CONFIG = window.AWSENGERS_AGENT_CONFIG || {
    apiUrl: "",
    authHeader: "Authorization",
    authToken: ""
};
